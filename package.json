{
  "name": "proxy-djay",
  "version": "1.0.0",
  "description": "",
  "main": "src/proxy-djay.ts",
  "scripts": {
    "start": "node ./dist/proxy-djay.js",
    "start:dev": "nodemon ./src/proxy-djay.ts",
    "build": "tsc",
    "build:bin": "mkdir -p dist/bin && pkg dist/proxy-djay.js --out-dir dist/bin && pushd dist/bin && for i in $(ls); do zip $i.zip $i;done && popd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/node": "^14.6.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "tslib": "^2.0.1",
    "config": "^3.3.1",
    "debug": "^4.1.1",
    "execa": "^4.0.3"
  }
}
